# build output
dist/
.astro/

# generated content — all three are written by one run of
# scripts/split-source.mjs, which `dev` and `build` invoke before Astro starts.
src/content/seasons/
src/data/references.json
src/data/search-index.json

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# environment variables
.env
.env.production
.env.local
.env.*.local

# macOS / Windows cruft
.DS_Store
Thumbs.db
desktop.ini

# editor / tooling caches
# .vscode is committed on purpose: it pins the workspace TypeScript and the
# Biome formatter so the editor matches what `npm run check` / `lint` do.
# Only genuinely per-user files are ignored.
.vscode/*.log
.history/
.idea/
*.swp
.cache/
.turbo/
.vercel/
